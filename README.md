# Semester 1 Major Project Suite

A centralized dashboard for engineering labs and project tools.

## Running the Project

1. Clone this repo.
2. Install requirements: `pip install -r requirements.txt`
3. Run the dashboard: `streamlit run master_main.py`

## Structure

- Each lab/module has its own folder
- `master_main.py`: main entry dashboard
- ...
